=====================================
STRIVER GRAPH SHEET - ALL QUESTIONS NOTES
=====================================

Format per question:
- Problem name
- What I did wrong
- Fixes applied
- Remaining improvements (optional)
- Tips (ask before writing)

=====================================

1. NO. OF PROVINCES
---
What I did wrong:
- Started DFS from cells with grid[i][j]==1 instead of from each unvisited node.

Fixes applied:
- Loop through every node i (0 to n-1). If !vis[i], run DFS from i and increment answer.
- This matches the standard connected-components pattern.

Remaining improvements:
- Passing adjacency matrix as const reference (done).
- Removed redundant early vis[i]=true marking coz its not needed (done).

Tips:
- Province count = number of DFS starts from unvisited nodes.

=====================================

2. ROTTEN ORANGES
---
What I did wrong:
- Using isAnyFreshLeft() function that does full-grid scan at end (unnecessary O(m*n) pass).

Fixes applied:
- Count fresh oranges initially and decrement fresh when rotting one. At end, check fresh==0 instead of rescanning.

Remaining improvements:
(ask before writing)

Tips:
(ask before writing)

=====================================

[Add more problems below as you solve them]

